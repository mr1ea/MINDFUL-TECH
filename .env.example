# Front-end environment variables
# Use this URL in the web app to point to the deployed backend.
VITE_API_BASE_URL=https://your-backend-url/api

# Application URL (used in backend for email links and frontend redirects)
APP_URL=https://your-frontend-url

# Optional: add any other front-end secrets here.
# Do not commit real API keys to source control.
